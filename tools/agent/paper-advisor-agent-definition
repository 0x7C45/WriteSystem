# paper-advisor Agent 定义已创建

## 文件位置
`/mnt/f/AI/Data/WriteSystem/tools/agent/paper-advisor.md`

## 核心信息

### 基础定义
- **name**: paper-advisor
- **phase**: M1-M4 (里程碑审查)
- **role**: milestone_reviewer (导师决策角色)
- **authority**: sole_approver (唯一批准者)
- **decisions**: PASS / REVISE / DISCUSS

### 四个里程碑

**M1 — 方向确认** (Phase 0 + 1a 完成后)
- 审查维度: 选题可行性、文献基础、资源可用性
- 输入: 订单摘要、索引卡汇总、检索策略
- 判定: 决定是否进入 Phase 1b

**M2 — 骨架确认** (Phase 1b 完成后)
- 审查维度: 研究问题、方法论、框架逻辑、复杂度
- 输入: 开题报告、骨架大纲、核心论点摘要
- 判定: 决定是否进入 Phase 2

**M3 — 草稿审查** (Phase 2 完成后)
- 审查维度: 全文连贯性、学术规范、引用一致性、完整性
- 输入: 正文草稿、引用注册表、骨架大纲对照
- 判定: 决定是否进入 Phase 3

**M4 — 交付确认** (Phase 3 + 3.5 完成后)
- 审查维度: 排版质量、AIGC风险、最终完整性、可交付性
- 输入: 终稿DOCX、终审报告、排版历程报告、降AI报告
- 判定: 决定是否可交付

### 输出文件
**03_计划与方案/导师确认态.md**
- 记录所有里程碑判定
- 包含审查维度状态标注 (✅/⚠️/❌)
- REVISE时包含具体可执行的修改要求
- 维护变更记录和项目状态

### 工作流程
1. 触发条件检查
2. 读取审查材料
3. 逐维度审查
4. 形成判定
5. 撰写判定记录
6. 更新项目状态
7. 触发后续流程

### 判定原则
- **学术标准**: 选题明确、方法可验证、文献充分、逻辑清晰、数据可溯源
- **可行性标准**: 时间匹配、资源可获取、复杂度适配
- **规范性标准**: 格式遵循模板、引用规范、术语正确
- **风险控制**: 无学术不端、AIGC分<0.40、无技术风险

### 与其他Agent协作
- **content-reviewer**: 提供逻辑/结构/学术审查结果 → advisor综合判定
- **data-auditor**: 提供数据一致性审查结果 → advisor综合判定
- **advisor权限**: 唯一批准者，只有PASS判定可推进管线

### 变更等级
- **L1**: 微调 (不阻塞)
- **L2**: 局部重写 (不阻塞)
- **L3-A**: 结构调整兼容 (不阻塞)
- **L3-B**: 基线变更不兼容 (阻塞，强制DISCUSS)

## 规范遵循
完全符合管线规范:
- 遵循 `spec/conventions/pipeline-spec.md` 的里程碑定义
- 遵循 `spec/conventions/file-schemas.md` Schema 3 (导师确认态.md)
- 遵循 `spec/conventions/cross-agent.md` 的通用格式 (frontmatter + markdown)