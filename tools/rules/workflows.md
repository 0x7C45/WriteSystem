# Workflow Strategy

> **[GENERAL RULE]** 此规则适用于所有开发任务，非论文撰写特定


## 复杂任务先分段，再决定是否委托

- 对复杂、高风险、跨文件、可并行的任务，先拆分阶段、依赖与验收标准。
- 简单任务直接执行，不强制生成阶段计划。
- 需要复用或交接时，再为阶段生成独立计划书与执行提示词。
- 是否使用子代理、是否并行、并行数量，由任务复杂度、依赖关系和冲突风险决定。
- 主对话负责统筹、集成和关键决策；必要时可直接执行，不强制只做调度。
